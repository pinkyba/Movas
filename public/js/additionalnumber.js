$(function () {

    var TotalValue = 0;
    $(".tr1 .loop1").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total1').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr2 .loop2").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total2').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr3 .loop3").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total3').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr4 .loop4").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total4').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr5 .loop5").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total5').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr6 .loop6").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total6').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr7 .loop7").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total7').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr7 .loop71").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total71').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr7 .loop72").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total72').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr8 .loop8").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total8').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr8 .loop81").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total81').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr8 .loop82").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total82').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr9 .loop9").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total9').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr10 .loop10").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total10').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr11 .loop11").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total11').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr12 .loop12").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total12').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr13 .loop131").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total131').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr13 .loop132").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total132').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr14 .loop141").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total141').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr14 .loop142").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total142').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr15 .loop15").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total15').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr16 .loop16").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total16').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr17 .loop17").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total17').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr18 .loop18").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total18').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr19 .loop19").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total19').innerHTML = TotalValue;
});


$(function () {

    var TotalValue = 0;
    $(".tr19 .loop191").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total191').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr19 .loop192").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total192').innerHTML = TotalValue;
});



$(function () {

    var TotalValue = 0;
    $(".tr19 .loop193").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total193').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr20 .loop20").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total20').innerHTML = TotalValue;
});



$(function () {

    var TotalValue = 0;
    $(".tr20 .loop201").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total201').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr20 .loop202").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total202').innerHTML = TotalValue;
});

$(function () {

    var TotalValue = 0;
    $(".tr20 .loop203").each(function (index, value) {
        currentRow = parseFloat($(this).text());
        TotalValue += currentRow
    });
    document.getElementById('total203').innerHTML = TotalValue;
});










